{
  "name": "tracker-lists",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker + KV store for shareable live favorite lists",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
